/**
 * Prisma client instance
 *
 * Re-exports the prisma client from the client module for convenience.
 * This allows for consistent imports across the application using @/lib/db/prisma
 */

export { prisma } from './client';
